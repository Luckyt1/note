---
created: 2026-08-24T19:43:02+08:00
project: bxi_rc_slam
project_path: /home/tang/tang_ws/nav/bxi_rc_slam
status: completed
tags:
  - ai-project
  - learning
---

# 恢复 Rotation Shim 每路径触发

## 本次完成
删除自定义 Rotation Shim 对相同目标的过滤，使每次收到 `FollowPath` 路径时都使用 Nav2 原生 `setPlan()` 重新触发初始朝向校正。

## 关键改动
- `src/bxi_nav/src/goal_rotation_shim_controller.cpp`：删除自定义 `setPlan()`，直接继承 Nav2 原生 Rotation Shim 行为。
- `src/bxi_nav/test/test_nav2_planning_contract.py`：增加契约，禁止再次覆盖 `setPlan()` 或引入目标过滤状态。

## 验证结果
- Rotation Shim 定向 pytest：1 passed，28 deselected。
- `colcon build --packages-select nav --symlink-install`：成功。
- 插件安装检查：安装空间链接到新构建库，源码/安装库哈希一致。

## 值得学习
- Nav2 原生 Rotation Shim 会在每次 `setPlan()` 时将 `path_updated_` 置真；按终点过滤重复路径会让相同目标的后续导航跳过初始旋转。

## 决策与取舍
- 保留现有自定义插件名称和打包接口，仅删除行为覆盖，避免扩大配置与部署变更范围。

## 后续事项
- 本次执行期间 `nav2_smac_mppi_params.yaml` 存在外部并发修改；已保留其 `rotate_to_heading_angular_vel: 1.2`、`vy_max/vy_std: 0.0` 等当前内容，未纳入本次验证结论。
