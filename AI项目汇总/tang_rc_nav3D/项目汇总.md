---
project: "tang_rc_nav3D"
project_path: "/home/tang/tang_ws/nav/tang_rc_nav3D"
updated: "2026-09-01T18:05:12+08:00"
tags:
  - ai-project-summary
  - learning
---

# tang_rc_nav3D 项目汇总

---

## 初始化 C++ 项目
<!-- obsidian-project-note-source: 2026-08-30_18-33-39_tang_rc_nav3D.md -->

- 创建时间：`2026-08-30T18:33:23+08:00`
- 原始笔记：`2026-08-30_18-33-39_tang_rc_nav3D.md`

### 本次完成
初始化了一个可直接配置、编译和运行的最小 C++20/CMake 项目，为后续功能开发提供可验证的基础结构。

### 关键改动
- `CMakeLists.txt`：声明 C++20 可执行目标 `tang_rc_nav3D`。
- `src/main.cpp`：添加输出问候语的程序入口。
- `.gitignore`：忽略本地 CMake 构建目录。

### 验证结果
- `cmake -S . -B build && cmake --build build && ./build/tang_rc_nav3D`：配置和编译成功，程序输出 `Hello, C++!`。

---

## 引入 ROS 2 Humble
<!-- obsidian-project-note-source: 2026-08-30_18-35-56_tang_rc_nav3D.md -->

- 创建时间：`2026-08-30T18:35:42+08:00`
- 原始笔记：`2026-08-30_18-35-56_tang_rc_nav3D.md`

### 本次完成
将原有 C++20/CMake 示例转换为可由 colcon 构建和 ros2 run 启动的 ROS 2 Humble ament_cmake 包。

### 关键改动
- `package.xml`：声明 ROS 包元数据以及 ament_cmake、rclcpp 依赖。
- `CMakeLists.txt`：接入 ament_cmake，构建并安装 ROS 2 节点。
- `src/main.cpp`：初始化 rclcpp 节点、输出启动日志并进入事件循环。
- `.gitignore`：忽略 colcon 的 build、install 和 log 目录。

### 验证结果
- `colcon build --packages-select tang_rc_nav3d`：构建成功，1 个包完成。
- `ros2 run tang_rc_nav3d tang_rc_nav3d_node`：节点成功启动并输出 `tang_rc_nav3d node started`。

---

## 完善 d435i_camera 构建配置
<!-- obsidian-project-note-source: 2026-08-30_18-56-48_tang_rc_nav3D.md -->

- 创建时间：`2026-08-30T18:56:33+08:00`
- 原始笔记：`2026-08-30_18-56-48_tang_rc_nav3D.md`

### 本次完成
补齐并简化 d435i_camera 的 CMake 配置，使发布和订阅节点可在 ROS 2 Humble 下独立编译安装。

### 关键改动
- `src/d435i_camera/CMakeLists.txt`：为两个目标声明 C++17，正确链接 OpenCV，保留实际 ROS 依赖，并移除无文件的安装规则。

### 验证结果
- `colcon build --base-paths src/d435i_camera --packages-select d435i_camera`：构建成功，1 个包完成。
- `colcon list --base-paths .`：仅发现根包 tang_rc_nav3d；嵌套包需通过独立 base path 构建或后续调整工作区布局。

---

## 修复 D435i 相机启动包
<!-- obsidian-project-note-source: 2026-08-30_19-28-35_tang_rc_nav3D.md -->

- 创建时间：`2026-08-30T19:28:17+08:00`
- 原始笔记：`2026-08-30_19-28-35_tang_rc_nav3D.md`

### 本次完成
修复 `d435i_camera` 无法正确启动真实 D435i 的问题，避免模拟发布器与真实驱动争用同名话题，并启用彩色、深度、IMU、深度对齐和点云。

### 关键改动
- `src/d435i_camera/launch/d435i_launch.py`：改为包装官方 RealSense 启动文件，使用当前驱动支持的参数名，只启动真实相机节点。
- `src/d435i_camera/package.xml`：补充启动文件实际使用的运行时依赖，移除不再默认启动的 RViz 依赖。

### 验证结果
- `colcon build --packages-select d435i_camera --symlink-install --cmake-clean-cache`：构建成功。
- `ros2 launch d435i_camera d435i_launch.py --show-args`：启动描述可正常加载，rosdep 检查通过。
- `rs-enumerate-devices`：识别到 Intel RealSense D435I（USB 3.2）。
- `timeout 8s ros2 launch d435i_camera d435i_launch.py`：真实设备成功打开彩色、深度、加速度计和陀螺仪，节点报告 `RealSense Node Is Up!`。

---

## 补充 D435i 版本一致性文档
<!-- obsidian-project-note-source: 2026-08-31_11-51-26_tang_rc_nav3D.md -->

- 创建时间：`2026-08-31T11:51:11+08:00`
- 原始笔记：`2026-08-31_11-51-26_tang_rc_nav3D.md`

### 本次完成
为 `d435i_camera` 增加 README，明确已验证的 RealSense ROS 版本组合、整组升级方式、运行时库检查、构建启动步骤和当前参数接口，避免混装版本或复用旧参数。

### 关键改动
- `src/d435i_camera/README.md`：记录 ROS wrapper 4.57.7 与 ROS librealsense 2.57.7 的已验证组合，并补充真实话题和版本排查命令。

### 验证结果
- `dpkg-query` 与 `ldd`：三个 RealSense ROS wrapper 包均为 4.57.7，节点实际链接 `/opt/ros/humble` 下的 librealsense 2.57.7。
- `rosdep check --from-paths src/d435i_camera --ignore-src`：依赖检查通过。
- 实机启动与 `ros2 topic list`：D435i 节点正常启动，README 中的 `/camera/camera/` 话题名称与实际发布结果一致。

---

## Odin 建图与地图保存启动脚本
<!-- obsidian-project-note-source: 2026-09-01_15-37-01_tang_rc_nav3D.md -->

- 创建时间：`2026-09-01T15:36:45+08:00`
- 原始笔记：`2026-09-01_15-37-01_tang_rc_nav3D.md`

### 本次完成
为 odin_ros_driver 增加交互式启动脚本，通过 1/2/3 完成启动 SLAM 建图、调用 ROS2 服务保存地图和安全停止退出。

### 关键改动
- `src/odin_ros_driver/start.sh`：自动加载工作区环境、校验 `custom_map_mode: 1`、管理 launch 进程与日志，并调用 `/odin1/save_map`。

### 验证结果
- `bash -n start.sh`：语法检查通过。
- `printf '9\n3\n' | ./start.sh`：无效输入与退出分支符合预期。
- `ros2 interface show odin_ros_driver/srv/SaveMap`：已安装的地图保存服务类型和脚本调用一致。

---

## 增加 Odin 静态 PLY 地图生成工具
<!-- obsidian-project-note-source: 2026-09-01_15-59-42_tang_rc_nav3D.md -->

- 创建时间：`2026-09-01T15:59:13+08:00`
- 原始笔记：`2026-09-01_15-59-42_tang_rc_nav3D.md`

### 本次完成
新增 `build_ply` ROS 2 工具，可实时累积 `/odin1/cloud_slam`，按体素去重并输出规划器可用的静态 PLY 点云；无点云输入时不会生成误导性的空地图。

### 关键改动
- `src/odin_ros_driver/script/build_ply.py`：解析 PointCloud2 的 XYZ/RGB 字段，支持二进制或 ASCII PLY、可选颜色、录制时长和体素尺寸。
- `src/odin_ros_driver/CMakeLists.txt`：安装为 `ros2 run odin_ros_driver build_ply`。
- `src/odin_ros_driver/package.xml`、`package_ros2.xml`：声明 `rclpy` 运行依赖。

### 验证结果
- `build_ply.py --self-test`：PointCloud2 解析、NaN 过滤、RGB 解包和二进制 PLY 写出通过。
- `colcon build --packages-select odin_ros_driver`：构建成功。
- `ros2 run odin_ros_driver build_ply --self-test`：安装入口验证通过。
- 真实话题烟雾测试：当前 ROS 图中无 `/odin1/cloud_slam` 发布者，正确返回无数据错误且不保留空 PLY。

---

## 移植并接入 SCAN-Planner
<!-- obsidian-project-note-source: 2026-09-01_17-07-31_tang_rc_nav3D.md -->

- 创建时间：`2026-09-01T17:07:15+08:00`
- 原始笔记：`2026-09-01_17-07-31_tang_rc_nav3D.md`

### 本次完成
将 3D-nav-dev 的 SCAN-Planner 最小依赖栈接入 Odin 里程计与 SLAM 点云，提供 RViz 目标输入、标准速度输出和工作区一键启动。

### 关键改动
- `src/{scan_planner,scan_planner_msgs,plan_env,path_searching,bspline_opt,traj_utils}`：移植规划节点、控制器、消息和算法依赖。
- `src/scan_planner/launch/odin_scan_planner.launch.py`：对接 `/odin1/odometry`、`/odin1/cloud_slam`、`/goal_pose` 与 `/cmd_vel`。
- `start.sh`：自动检查/构建缺失包并联合启动 Odin、规划器和闭环控制器。

### 验证结果
- `colcon build --packages-select ...`：6 个规划器包编译通过。
- `colcon test-result --verbose`：5 项测试，0 错误、0 失败。
- `rosdep check ... --ignore-src`：系统依赖全部满足。
- 短时 launch smoke test：规划节点和闭环控制器均成功启动并干净退出；未连接真实底盘验证运动。

---

## 区分建图、重定位与导航模式
<!-- obsidian-project-note-source: 2026-09-01_17-24-42_tang_rc_nav3D.md -->

- 创建时间：`2026-09-01T17:24:24+08:00`
- 原始笔记：`2026-09-01_17-24-42_tang_rc_nav3D.md`

### 本次完成
将统一启动入口拆分为建图、重定位和导航三种明确模式，导航强制加载 Odin `.bin` 地图完成重定位后再启动规划与控制。

### 关键改动
- `start.sh`：提供交互菜单及 `mapping`、`localization`、`navigation` 三个命令；建图支持同终端保存并保护传输过程。
- `src/scan_planner/launch/odin_scan_planner.launch.py`：按模式生成独立运行时 Odin 配置，并分别控制规划器和控制器启停。
- `src/scan_planner/test/test_odin_mode_config.py`：验证模式值、地图路径和缺失地图拒绝行为。

### 验证结果
- `colcon build --packages-select scan_planner`：构建通过。
- `colcon test --packages-select scan_planner`：7 项测试，0 错误、0 失败。
- `bash -n start.sh` 与三种 CLI 参数检查：通过；未连接真实 Odin/底盘执行运动。

---

## 修复 Odin 重定位配置未生效
<!-- obsidian-project-note-source: 2026-09-01_18-05-26_tang_rc_nav3D.md -->

- 创建时间：`2026-09-01T18:05:12+08:00`
- 原始笔记：`2026-09-01_18-05-26_tang_rc_nav3D.md`

### 本次完成
修复启动脚本生成的 mode 2 动态配置未被 Odin 驱动读取的问题，使重定位模式和 `.bin` 地图路径能够真正下发给设备。

### 关键改动
- `src/odin_ros_driver/src/host_sdk_sample.cpp`：ROS 2 下读取 launch 传入的 `config_file` 参数，并记录实际加载路径；缺省时仍使用原 `control_command.yaml`。

### 验证结果
- `colcon build --packages-select odin_ros_driver`：编译成功。
- 配置参数探针：驱动日志显示读取传入的 `/tmp/odin_config_parameter_probe_missing.yaml`，证明参数覆盖链路生效。
- `colcon test --packages-select scan_planner`：4 项测试，0 错误、0 失败。
