---
project: "tang_rc_nav3D"
project_path: "/home/tang/tang_ws/nav/tang_rc_nav3D"
updated: "2026-09-02T18:02:57+08:00"
tags:
  - ai-project-summary
  - learning
---

# tang_rc_nav3D 项目汇总

---

## 初始化 C++ 项目
<!-- obsidian-project-note-source: 2026-08-30_18-33-39_tang_rc_nav3D.md -->

- 创建时间：`2026-08-30T18:33:23+08:00`
- 原始笔记：`2026-08-30_18-33-39_tang_rc_nav3D.md`

### 本次完成
初始化了一个可直接配置、编译和运行的最小 C++20/CMake 项目，为后续功能开发提供可验证的基础结构。

### 关键改动
- `CMakeLists.txt`：声明 C++20 可执行目标 `tang_rc_nav3D`。
- `src/main.cpp`：添加输出问候语的程序入口。
- `.gitignore`：忽略本地 CMake 构建目录。

### 验证结果
- `cmake -S . -B build && cmake --build build && ./build/tang_rc_nav3D`：配置和编译成功，程序输出 `Hello, C++!`。

---

## 引入 ROS 2 Humble
<!-- obsidian-project-note-source: 2026-08-30_18-35-56_tang_rc_nav3D.md -->

- 创建时间：`2026-08-30T18:35:42+08:00`
- 原始笔记：`2026-08-30_18-35-56_tang_rc_nav3D.md`

### 本次完成
将原有 C++20/CMake 示例转换为可由 colcon 构建和 ros2 run 启动的 ROS 2 Humble ament_cmake 包。

### 关键改动
- `package.xml`：声明 ROS 包元数据以及 ament_cmake、rclcpp 依赖。
- `CMakeLists.txt`：接入 ament_cmake，构建并安装 ROS 2 节点。
- `src/main.cpp`：初始化 rclcpp 节点、输出启动日志并进入事件循环。
- `.gitignore`：忽略 colcon 的 build、install 和 log 目录。

### 验证结果
- `colcon build --packages-select tang_rc_nav3d`：构建成功，1 个包完成。
- `ros2 run tang_rc_nav3d tang_rc_nav3d_node`：节点成功启动并输出 `tang_rc_nav3d node started`。

---

## 完善 d435i_camera 构建配置
<!-- obsidian-project-note-source: 2026-08-30_18-56-48_tang_rc_nav3D.md -->

- 创建时间：`2026-08-30T18:56:33+08:00`
- 原始笔记：`2026-08-30_18-56-48_tang_rc_nav3D.md`

### 本次完成
补齐并简化 d435i_camera 的 CMake 配置，使发布和订阅节点可在 ROS 2 Humble 下独立编译安装。

### 关键改动
- `src/d435i_camera/CMakeLists.txt`：为两个目标声明 C++17，正确链接 OpenCV，保留实际 ROS 依赖，并移除无文件的安装规则。

### 验证结果
- `colcon build --base-paths src/d435i_camera --packages-select d435i_camera`：构建成功，1 个包完成。
- `colcon list --base-paths .`：仅发现根包 tang_rc_nav3d；嵌套包需通过独立 base path 构建或后续调整工作区布局。

---

## 修复 D435i 相机启动包
<!-- obsidian-project-note-source: 2026-08-30_19-28-35_tang_rc_nav3D.md -->

- 创建时间：`2026-08-30T19:28:17+08:00`
- 原始笔记：`2026-08-30_19-28-35_tang_rc_nav3D.md`

### 本次完成
修复 `d435i_camera` 无法正确启动真实 D435i 的问题，避免模拟发布器与真实驱动争用同名话题，并启用彩色、深度、IMU、深度对齐和点云。

### 关键改动
- `src/d435i_camera/launch/d435i_launch.py`：改为包装官方 RealSense 启动文件，使用当前驱动支持的参数名，只启动真实相机节点。
- `src/d435i_camera/package.xml`：补充启动文件实际使用的运行时依赖，移除不再默认启动的 RViz 依赖。

### 验证结果
- `colcon build --packages-select d435i_camera --symlink-install --cmake-clean-cache`：构建成功。
- `ros2 launch d435i_camera d435i_launch.py --show-args`：启动描述可正常加载，rosdep 检查通过。
- `rs-enumerate-devices`：识别到 Intel RealSense D435I（USB 3.2）。
- `timeout 8s ros2 launch d435i_camera d435i_launch.py`：真实设备成功打开彩色、深度、加速度计和陀螺仪，节点报告 `RealSense Node Is Up!`。

---

## 补充 D435i 版本一致性文档
<!-- obsidian-project-note-source: 2026-08-31_11-51-26_tang_rc_nav3D.md -->

- 创建时间：`2026-08-31T11:51:11+08:00`
- 原始笔记：`2026-08-31_11-51-26_tang_rc_nav3D.md`

### 本次完成
为 `d435i_camera` 增加 README，明确已验证的 RealSense ROS 版本组合、整组升级方式、运行时库检查、构建启动步骤和当前参数接口，避免混装版本或复用旧参数。

### 关键改动
- `src/d435i_camera/README.md`：记录 ROS wrapper 4.57.7 与 ROS librealsense 2.57.7 的已验证组合，并补充真实话题和版本排查命令。

### 验证结果
- `dpkg-query` 与 `ldd`：三个 RealSense ROS wrapper 包均为 4.57.7，节点实际链接 `/opt/ros/humble` 下的 librealsense 2.57.7。
- `rosdep check --from-paths src/d435i_camera --ignore-src`：依赖检查通过。
- 实机启动与 `ros2 topic list`：D435i 节点正常启动，README 中的 `/camera/camera/` 话题名称与实际发布结果一致。

---

## Odin 建图与地图保存启动脚本
<!-- obsidian-project-note-source: 2026-09-01_15-37-01_tang_rc_nav3D.md -->

- 创建时间：`2026-09-01T15:36:45+08:00`
- 原始笔记：`2026-09-01_15-37-01_tang_rc_nav3D.md`

### 本次完成
为 odin_ros_driver 增加交互式启动脚本，通过 1/2/3 完成启动 SLAM 建图、调用 ROS2 服务保存地图和安全停止退出。

### 关键改动
- `src/odin_ros_driver/start.sh`：自动加载工作区环境、校验 `custom_map_mode: 1`、管理 launch 进程与日志，并调用 `/odin1/save_map`。

### 验证结果
- `bash -n start.sh`：语法检查通过。
- `printf '9\n3\n' | ./start.sh`：无效输入与退出分支符合预期。
- `ros2 interface show odin_ros_driver/srv/SaveMap`：已安装的地图保存服务类型和脚本调用一致。

---

## 增加 Odin 静态 PLY 地图生成工具
<!-- obsidian-project-note-source: 2026-09-01_15-59-42_tang_rc_nav3D.md -->

- 创建时间：`2026-09-01T15:59:13+08:00`
- 原始笔记：`2026-09-01_15-59-42_tang_rc_nav3D.md`

### 本次完成
新增 `build_ply` ROS 2 工具，可实时累积 `/odin1/cloud_slam`，按体素去重并输出规划器可用的静态 PLY 点云；无点云输入时不会生成误导性的空地图。

### 关键改动
- `src/odin_ros_driver/script/build_ply.py`：解析 PointCloud2 的 XYZ/RGB 字段，支持二进制或 ASCII PLY、可选颜色、录制时长和体素尺寸。
- `src/odin_ros_driver/CMakeLists.txt`：安装为 `ros2 run odin_ros_driver build_ply`。
- `src/odin_ros_driver/package.xml`、`package_ros2.xml`：声明 `rclpy` 运行依赖。

### 验证结果
- `build_ply.py --self-test`：PointCloud2 解析、NaN 过滤、RGB 解包和二进制 PLY 写出通过。
- `colcon build --packages-select odin_ros_driver`：构建成功。
- `ros2 run odin_ros_driver build_ply --self-test`：安装入口验证通过。
- 真实话题烟雾测试：当前 ROS 图中无 `/odin1/cloud_slam` 发布者，正确返回无数据错误且不保留空 PLY。

---

## 移植并接入 SCAN-Planner
<!-- obsidian-project-note-source: 2026-09-01_17-07-31_tang_rc_nav3D.md -->

- 创建时间：`2026-09-01T17:07:15+08:00`
- 原始笔记：`2026-09-01_17-07-31_tang_rc_nav3D.md`

### 本次完成
将 3D-nav-dev 的 SCAN-Planner 最小依赖栈接入 Odin 里程计与 SLAM 点云，提供 RViz 目标输入、标准速度输出和工作区一键启动。

### 关键改动
- `src/{scan_planner,scan_planner_msgs,plan_env,path_searching,bspline_opt,traj_utils}`：移植规划节点、控制器、消息和算法依赖。
- `src/scan_planner/launch/odin_scan_planner.launch.py`：对接 `/odin1/odometry`、`/odin1/cloud_slam`、`/goal_pose` 与 `/cmd_vel`。
- `start.sh`：自动检查/构建缺失包并联合启动 Odin、规划器和闭环控制器。

### 验证结果
- `colcon build --packages-select ...`：6 个规划器包编译通过。
- `colcon test-result --verbose`：5 项测试，0 错误、0 失败。
- `rosdep check ... --ignore-src`：系统依赖全部满足。
- 短时 launch smoke test：规划节点和闭环控制器均成功启动并干净退出；未连接真实底盘验证运动。

---

## 区分建图、重定位与导航模式
<!-- obsidian-project-note-source: 2026-09-01_17-24-42_tang_rc_nav3D.md -->

- 创建时间：`2026-09-01T17:24:24+08:00`
- 原始笔记：`2026-09-01_17-24-42_tang_rc_nav3D.md`

### 本次完成
将统一启动入口拆分为建图、重定位和导航三种明确模式，导航强制加载 Odin `.bin` 地图完成重定位后再启动规划与控制。

### 关键改动
- `start.sh`：提供交互菜单及 `mapping`、`localization`、`navigation` 三个命令；建图支持同终端保存并保护传输过程。
- `src/scan_planner/launch/odin_scan_planner.launch.py`：按模式生成独立运行时 Odin 配置，并分别控制规划器和控制器启停。
- `src/scan_planner/test/test_odin_mode_config.py`：验证模式值、地图路径和缺失地图拒绝行为。

### 验证结果
- `colcon build --packages-select scan_planner`：构建通过。
- `colcon test --packages-select scan_planner`：7 项测试，0 错误、0 失败。
- `bash -n start.sh` 与三种 CLI 参数检查：通过；未连接真实 Odin/底盘执行运动。

---

## 修复 Odin 重定位配置未生效
<!-- obsidian-project-note-source: 2026-09-01_18-05-26_tang_rc_nav3D.md -->

- 创建时间：`2026-09-01T18:05:12+08:00`
- 原始笔记：`2026-09-01_18-05-26_tang_rc_nav3D.md`

### 本次完成
修复启动脚本生成的 mode 2 动态配置未被 Odin 驱动读取的问题，使重定位模式和 `.bin` 地图路径能够真正下发给设备。

### 关键改动
- `src/odin_ros_driver/src/host_sdk_sample.cpp`：ROS 2 下读取 launch 传入的 `config_file` 参数，并记录实际加载路径；缺省时仍使用原 `control_command.yaml`。

### 验证结果
- `colcon build --packages-select odin_ros_driver`：编译成功。
- 配置参数探针：驱动日志显示读取传入的 `/tmp/odin_config_parameter_probe_missing.yaml`，证明参数覆盖链路生效。
- `colcon test --packages-select scan_planner`：4 项测试，0 错误、0 失败。

---

## 接入 PCD 与 SCAN-Planner 导航可视化
<!-- obsidian-project-note-source: 2026-09-02_11-26-21_tang_rc_nav3D.md -->

- 创建时间：`2026-09-02T11:25:43+08:00`
- 原始笔记：`2026-09-02_11-26-21_tang_rc_nav3D.md`

### 本次完成
导航和重定位模式支持可选加载 PCD 静态参考地图；RViz 默认显示 SCAN-Planner 的局部占据体素、膨胀区域、滑窗边界、全局参考路径和局部优化轨迹，便于判断机器人预期路线。

### 关键改动
- `start.sh`：模式 2/3 支持选择 `.bin` 后再选择可选 `.pcd`，同目录同名 PCD 会自动加载。
- `src/scan_planner/launch/odin_scan_planner.launch.py`：复用 `pcl_ros/pcd_to_pointcloud` 发布 `/static_map_cloud`。
- `src/odin_ros_driver/config/odin_ros2.rviz`：增加静态 PCD、局部栅格和规划路径显示组。
- `src/traj_utils/src/planning_visualization.cpp`：规划 Marker 使用配置的 `grid_map.frame_id`，避免硬编码 `world` 导致 Odin 模式下不可见。
- `src/scan_planner/test/test_odin_mode_config.py`：覆盖 PCD 路径校验和 RViz 必需话题。

### 验证结果
- `colcon build --packages-select traj_utils scan_planner`：构建成功。
- 测试 PCD 启动检查：成功发布 2 个点到 `/static_map_cloud`，frame 为 `map`。
- `colcon test --packages-select scan_planner`：6 项测试，0 错误、0 失败。
- `bash -n start.sh` 与 RViz YAML 解析：均通过。

---

## 启动脚本默认扫描根目录 maps
<!-- obsidian-project-note-source: 2026-09-02_11-34-34_tang_rc_nav3D.md -->

- 创建时间：`2026-09-02T11:34:05+08:00`
- 原始笔记：`2026-09-02_11-34-34_tang_rc_nav3D.md`

### 本次完成
重定位和导航启动时默认递归扫描项目根目录 `maps/` 中的 BIN 与 PCD 地图；单个文件自动选择，多个文件按编号选择，并保留显式路径启动方式。

### 关键改动
- `start.sh`：新增 `MAPS_DIR` 和统一文件选择逻辑，支持 `maps/Bin`、`maps/PCD` 等子目录以及带空格路径；同名 PCD 仍优先自动配对。

### 验证结果
- `bash -n start.sh`：语法通过。
- 默认地图选择检查：自动找到 `maps/Bin/map_20260901_190441.bin` 和 `maps/PCD/Relocation Map.pcd`。
- `./start.sh --help`：正确显示默认地图目录和递归扫描说明。

---

## 统一 Odin 与静态地图时间轴
<!-- obsidian-project-note-source: 2026-09-02_13-18-34_tang_rc_nav3D.md -->

- 创建时间：`2026-09-02T13:18:18+08:00`
- 原始笔记：`2026-09-02_13-18-34_tang_rc_nav3D.md`

### 本次完成
将 Odin ROS 消息切换为主机 ROS 时间，使设备点云、里程计、动态 TF 与主机发布的静态 PCD 使用同一时间轴，避免 RViz 因找不到对应时刻的坐标变换而持续丢弃地图消息。

### 关键改动
- `src/odin_ros_driver/config/control_command.yaml`：将 `use_host_ros_time` 从 `0` 改为 `1`，并重新构建安装 `odin_ros_driver`。

### 验证结果
- `colcon build --packages-select odin_ros_driver`：构建成功。
- `python3 -m pytest -q src/scan_planner/test/test_odin_mode_config.py`：3 项测试通过。
- 安装配置检查：源码与 `install` 配置一致，`use_host_ros_time=1`。

---

## Odin 倾斜安装与 base_link 里程计接入
<!-- obsidian-project-note-source: 2026-09-02_14-35-35_tang_rc_nav3D.md -->

- 创建时间：`2026-09-02T14:34:58+08:00`
- 原始笔记：`2026-09-02_14-35-35_tang_rc_nav3D.md`

### 本次完成
将与机器人原点重合、绕 Y 轴向下俯视 45° 的 Odin 安装关系接入导航链路。规划器和控制器改用修正后的 `base_link` 里程计，GridMap 保留原始 `imu` 里程计用于传感器射线计算；同时保持 Odin 与主机发布地图使用同一 ROS 时间轴。

### 关键改动
- `src/scan_planner/scripts/base_link_odometry.py`：新增 Odin 到 `base_link` 的姿态、速度及 twist 协方差转换，并发布固定安装 TF。
- `src/scan_planner/launch/odin_scan_planner.launch.py`：新增安装角和 base 里程计参数，将 planner/controller 接入 `/base_link/odometry`。
- `src/scan_planner/test/test_base_link_odometry.py`：覆盖 45° 安装姿态、速度方向、输出 frame 与协方差旋转。
- `src/scan_planner/CMakeLists.txt`：安装转换节点并注册测试。
- `src/odin_ros_driver/config/control_command.yaml`：启用主机 ROS 时间，统一 Odin 数据与静态 PCD 的时间轴。

### 验证结果
- `python3 -m pytest -q src/scan_planner/test/test_base_link_odometry.py src/scan_planner/test/test_odin_mode_config.py`：5 项通过。
- `colcon build --packages-select scan_planner`：构建成功。
- `colcon test --packages-select scan_planner`：9 tests，0 errors，0 failures。
- `ros2 launch scan_planner odin_scan_planner.launch.py --show-args`：launch 参数解析成功，安装脚本已安装且可执行。

---

## ScanPlanner 参数补充中文说明
<!-- obsidian-project-note-source: 2026-09-02_14-49-24_tang_rc_nav3D.md -->

- 创建时间：`2026-09-02T14:48:54+08:00`
- 原始笔记：`2026-09-02_14-49-24_tang_rc_nav3D.md`

### 本次完成
为 ScanPlanner 规划、地图、轨迹优化、控制器、仿真和 Odin 启动参数补充中文用途、单位与覆盖关系说明，未修改任何参数值或运行逻辑。

### 关键改动
- `src/scan_planner/config/planner.yaml`：为状态机、三维栅格地图、传感器、概率地图和轨迹优化参数添加中文注释。
- `src/scan_planner/config/controllers.yaml`：为闭环控制、开环控制、运动学仿真和步态参数添加中文注释。
- `src/scan_planner/launch/odin_scan_planner.launch.py`：为全部 launch 参数添加中文 description，并标明 launch 对 YAML 同名参数的覆盖关系。

### 验证结果
- `yaml.safe_load`：两个 YAML 配置文件解析成功。
- `python3 -m py_compile src/scan_planner/launch/odin_scan_planner.launch.py`：语法检查通过。
- `ros2 launch scan_planner odin_scan_planner.launch.py --show-args`：所有启动参数正常显示中文说明。
- `colcon build --packages-select scan_planner`：构建成功。
- `colcon test --packages-select scan_planner`：9 tests，0 errors，0 failures。

---

## 同步 Octo 全局规划与 ScanPlanner
<!-- obsidian-project-note-source: 2026-09-02_15-13-27_tang_rc_nav3D.md -->

- 创建时间：`2026-09-02T15:12:54+08:00`
- 原始笔记：`2026-09-02_15-13-27_tang_rc_nav3D.md`

### 本次完成
将现有 `bxi_octo_global_planner` 接入 Odin 导航入口：全局规划器使用静态 PCD、修正后的 `base_link` 里程计和 RViz 目标生成 `/initial_path`，ScanPlanner 以模式 3 消费全局路径并继续负责局部避障与轨迹优化。

### 关键改动
- `src/scan_planner/launch/odin_scan_planner.launch.py`：新增全局规划启动、导航模式、全局路径及 OctoMap 分辨率参数，统一 PCD/global frame、机器人半径、目标和里程计话题。
- `start.sh`：导航模式自动构建并启动 Octo 全局规划器，要求提供 PCD，并将 ScanPlanner 切换为 `navi_mode=3`。
- `src/scan_planner/package.xml`：声明全局规划器运行依赖。
- `src/scan_planner/test/test_odin_mode_config.py`：增加全局规划启用时的 PCD 校验测试。
- `src/scan_planner/scripts/base_link_odometry.py`：避免 ROS Ctrl-C 时重复清理产生异常栈。

### 验证结果
- `python3 -m pytest`：相关单元测试 6 项通过。
- `colcon build --packages-select bxi_octo_global_planner scan_planner`：两个包构建成功。
- `colcon test --packages-select bxi_octo_global_planner scan_planner`：全局规划器 1 项、ScanPlanner 10 项测试全部通过。
- 真实 `Relocation Map.pcd` 烟雾测试：356495 点加载成功，OctoMap 构建完成；全局规划器就绪话题为 `odom=/base_link/odometry`、`goal=/goal_pose`、`path=/initial_path`，ScanPlanner 模式 3 同时正常启动。

---

## 接入并显示 Octo 全局规划路线
<!-- obsidian-project-note-source: 2026-09-02_18-03-21_tang_rc_nav3D.md -->

- 创建时间：`2026-09-02T18:02:57+08:00`
- 原始笔记：`2026-09-02_18-03-21_tang_rc_nav3D.md`

### 本次完成
复用现有 Octo 全局 A* 导航链路，将机器人半径同步给全局规划器，并在默认 RViz 中显示全局路线，便于区分全局引导与局部优化轨迹。

### 关键改动
- `src/scan_planner/launch/odin_scan_planner.launch.py`：向 Octo 全局规划器传递统一的 `robot_radius`。
- `src/odin_ros_driver/config/odin_ros2.rviz`：新增 `/octo_global_path` 蓝色全局 A* 路线显示。
- `src/scan_planner/test/test_odin_mode_config.py`：验证全局半径传递和 RViz 全局路径话题。

### 验证结果
- `/usr/bin/python3 -m pytest -q src/scan_planner/test`：8 项测试全部通过。
- `colcon build --packages-select odin_ros_driver scan_planner`：两个包构建成功，仅有既有编译警告。
- 无硬件短时 launch 冒烟检查：OctoMap 构建成功，全局规划器进入 ready 状态。
