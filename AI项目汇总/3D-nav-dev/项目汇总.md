---
project: "3D-nav-dev"
project_path: "/home/tang/tang_ws/3D-nav-dev"
updated: "2026-08-28T18:51:23+08:00"
tags:
  - ai-project-summary
  - learning
---

# 3D-nav-dev 项目汇总

---

## 增加一键仿真测试启停脚本
<!-- obsidian-project-note-source: 2026-08-25_15-06-45_3D-nav-dev.md -->

- 创建时间：`2026-08-25T11:47:59+08:00`
- 原始笔记：`2026-08-25_15-06-45_3D-nav-dev.md`

### 本次完成
增加一键启动和停止入口，自动复用现有 ELF3 仿真策略与 3D 建图脚本，并用独立进程组、PID 文件和日志文件管理后台测试进程。

### 关键改动
- `start.sh`：缺少完整安装产物时自动构建，依次启动仿真策略和建图，阻止重复启动并在失败时清理已启动进程。
- `stop.sh`：按 PID 文件向整个进程组发送 TERM，超时后发送 KILL，支持重复执行。

### 验证结果
- `bash -n start.sh stop.sh`：通过，两个脚本语法正确且具有执行权限。
- 隔离 `sleep` 进程组冒烟：真实会话 PID 写入、重复启动拦截、失败清理和停止后 PID 文件删除均通过。
- `./start.sh`：首次构建被现有环境依赖阻断；缺少 Pangolin，且 PCL/VTK 引用的 `MPI::MPI_C` 目标不存在，未进入 ROS 全链路启动。
- 残留检查：无 `colcon build`、目标 ROS launch 或运行 PID 文件残留。

---

## 编译 3D-nav-dev 并补齐依赖
<!-- obsidian-project-note-source: 2026-08-25_15-26-38_3D-nav-dev.md -->

- 创建时间：`2026-08-25T15:26:07+08:00`
- 原始笔记：`2026-08-25_15-26-38_3D-nav-dev.md`

### 本次完成
补齐 ROS 2 Humble 运行/编译依赖并完成工作区构建，15 个项目包全部通过。解决了 Pangolin/RTAB-Map 依赖缺失，以及 PCL/VTK 查找 OpenMPI C 目标失败两个阻塞点。

### 关键改动
- `src/bxi_example_nav3d/bxi_octo_global_planner/CMakeLists.txt`：为项目同时启用 C 与 CXX，使 PCL/VTK 能创建并链接 `MPI::MPI_C`。
- 系统依赖：安装 Pangolin、depth-image-proc、RTAB-Map launch/odom/slam/sync/msgs/rviz 插件和 imu-filter-madgwick 等 ROS 包及传递依赖。

### 验证结果
- `./build.sh`：全量构建 15/15 成功；再次增量构建 15/15 成功。
- `ros2 pkg prefix bxi_example_py_elf3 bxi_scan_nav bxi_orbslam3_ros2`：三个关键包均解析到本工作区 `install`。
- `colcon test --merge-install --packages-select scan_planner`（`ROS_LOG_DIR=/tmp/3d-nav-ros-logs`）：2/2 通过。
- `ctest --test-dir build/remote_controller -R 'remote_controller_(crsf_frame_parser|control_rules)_test'`：2/2 核心单元测试通过。
- `rosdep check --from-paths src --ignore-src -r`：系统依赖均满足；仅 `ament_python` 在当前 rosdep 数据中无映射。

---

## 恢复启动实时日志与 RViz 点云
<!-- obsidian-project-note-source: 2026-08-25_16-36-15_3D-nav-dev.md -->

- 创建时间：`2026-08-25T16:35:49+08:00`
- 原始笔记：`2026-08-25_16-36-15_3D-nav-dev.md`

### 本次完成
让 `start.sh` 启动后持续输出策略与建图日志，并修复 RTAB-Map 组件无法加载、RViz 点云 QoS 不兼容导致的空白显示。

### 关键改动
- `start.sh`：启动后使用 `tail -F` 实时跟随两份日志；系统缺失新版本动态库时加载项目内本地 ROS overlay。
- `src/bxi_example_nav3d/bxi_scan_nav/config/elf3_rtab_mapping.rviz`：将 `/rtabmap/cloud_map` 的 Durability 改为 `Volatile`，与发布端一致。

### 验证结果
- `bash -n start.sh`：通过。
- `colcon build --merge-install --packages-select bxi_scan_nav`：构建通过。
- `ros2 topic info /rtabmap/cloud_map --verbose`：发布端与 RViz 订阅端均为 Reliable + Volatile，订阅者为 1。
- `ros2 topic echo --once /rtabmap/cloud_map --field header`：收到 `frame_id: world` 的实时消息。

---

## 增加 3D 导航快速入口
<!-- obsidian-project-note-source: 2026-08-27_15-43-15_3D-nav-dev.md -->

- 创建时间：`2026-08-27T15:42:57+08:00`
- 原始笔记：`2026-08-27_15-43-15_3D-nav-dev.md`

### 本次完成
增加一个统一命令入口，复用现有 ROS 2 构建、仿真、建图、导航和地图导出脚本，并修复首次构建时 PCL/VTK 找不到 MPI C 目标的问题。

### 关键改动
- `quickstart.sh`：统一提供 build、check、sim-*、hw-*、keyboard 和 export 命令，真机命令要求 root。
- `build.sh`：允许将额外参数传给 colcon。
- `README.md`：补充快速入口和仿真建图示例。
- `src/bxi_example_nav3d/{bxi_octo_global_planner,plan_env}/CMakeLists.txt`：在 PCL 之前显式加载 MPI C。

### 验证结果
- `./quickstart.sh build --cmake-clean-cache`：15 个 ROS 2 包全部构建成功。
- `colcon test --merge-install --packages-select bxi_octo_global_planner`：1 个测试通过。
- `./quickstart.sh check`：依赖、可执行文件、动态库和可移植性检查全部通过。
- `bash -n quickstart.sh build.sh src/bxi_example_nav3d/scripts/*.sh`：脚本语法检查通过。

---

## RTAB-Map 改用仿真原生头部相机
<!-- obsidian-project-note-source: 2026-08-27_18-16-31_3D-nav-dev.md -->

- 创建时间：`2026-08-27T18:16:15+08:00`
- 原始笔记：`2026-08-27_18-16-31_3D-nav-dev.md`

### 本次完成
仿真建图入口不再重复启动 RGB-D 渲染节点，RTAB-Map 直接订阅仿真已发布的头部相机数据，避免重复渲染和相机来源混淆。

### 关键改动
- `src/bxi_example_nav3d/scripts/run_elf3_sim_mapping.sh`：关闭 `start_simulated_cameras`，将 RGB、深度和 CameraInfo 输入切换到 `/simulation/head_depth_camera/*`。

### 验证结果
- `bash -n src/bxi_example_nav3d/scripts/run_elf3_sim_mapping.sh`：脚本语法检查通过。
- `ros2 launch bxi_scan_nav elf3_rtab_cuvslam_nav.launch.py --show-args`：确认四个覆盖参数均为有效 launch 参数。

---

## 修复 Colcon 重复包发现错误
<!-- obsidian-project-note-source: 2026-08-28_13-55-19_3D-nav-dev.md -->

- 创建时间：`2026-08-28T13:54:21+08:00`
- 原始笔记：`2026-08-28_13-55-19_3D-nav-dev.md`

### 本次完成
消除了外层工作区与 `src/bxi_controller_ros2` 嵌套工作区之间的四组同名 ROS 2 包冲突，同时保留嵌套目录中唯一的仿真包 `bxi_example_py_elf3`。

### 关键改动
- `src/bxi_controller_ros2/src/bxi_depth_camera/COLCON_IGNORE`：忽略嵌套的相机包，使用外层版本。
- `src/bxi_controller_ros2/src/bxi_example_bms/COLCON_IGNORE`：忽略嵌套的 BMS 包，使用外层版本。
- `src/bxi_controller_ros2/src/remote_controller/COLCON_IGNORE`：忽略嵌套的遥控包，使用外层版本。
- `src/bxi_controller_ros2/src/remote_py_test/COLCON_IGNORE`：忽略嵌套的测试包，使用外层版本。

### 验证结果
- `colcon list --names-only`：成功发现 15 个唯一包，无重复包名，并保留 `bxi_example_py_elf3`。
- `bash build.sh`：已越过原 Duplicate package names 错误并开始构建；沙箱内 OpenMPI 初始化因禁止 socket 而导致 MPI 探测失败，属于验证环境限制。

---

## 修复 PCL 加载 VTK 时的 CMake 配置失败
<!-- obsidian-project-note-source: 2026-08-28_14-55-23_3D-nav-dev.md -->

- 创建时间：`2026-08-28T14:55:10+08:00`
- 原始笔记：`2026-08-28_14-55-23_3D-nav-dev.md`

### 本次完成
修复 bxi_octo_global_planner 在 CMake 3.28 下查找 PCL/VTK 时缺少 MPI::MPI_C 目标的问题，并消除 PCL 旧配置触发的 CMP0144 开发者警告。

### 关键改动
- `src/bxi_example_nav3d/bxi_octo_global_planner/CMakeLists.txt`：启用 C 语言以生成 VTK 所需的 MPI C 导入目标，并为 PCL 的旧策略作用域设置 CMP0144 默认行为。

### 验证结果
- `cmake -S ... -B /tmp/bxi-octo-final-check -DBUILD_TESTING=ON`：配置与生成成功，未再出现 CMP0144 或 MPI::MPI_C 错误。
- `cmake --build /tmp/bxi-octo-final-check --parallel 2`：节点与测试目标构建成功。
- `ctest --test-dir /tmp/bxi-octo-final-check --output-on-failure`：1/1 测试通过。

---

## 生成 ELF3 三维导航运行时架构图
<!-- obsidian-project-note-source: 2026-08-28_15-26-10_3D-nav-dev.md -->

- 创建时间：`2026-08-28T15:25:43+08:00`
- 原始笔记：`2026-08-28_15-26-10_3D-nav-dev.md`

### 本次完成
基于当前 ROS 2 代码与启动入口，生成中文高层运行时架构图，覆盖核心调用链、地图持久化、内存体素缓存、外部依赖和信任边界。

### 关键改动
- `docs/runtime-architecture.zh-CN.json`：新增可复现的 Archify 架构规范。
- `docs/runtime-architecture.zh-CN.html`：新增支持主题切换、缩放、搜索和路径聚焦的独立交互式架构图。

### 验证结果
- `archify validate --quality showcase`：9/9 检查通过，0 错误、0 警告。
- `archify visual-check`：1440×900、1600×1000、1920×1080、2048×1320 均无溢出；明暗主题截图人工检查通过。

---

## 同步 ELF3 MuJoCo 默认环境
<!-- obsidian-project-note-source: 2026-08-28_16-12-25_3D-nav-dev.md -->

- 创建时间：`2026-08-28T16:11:51+08:00`
- 原始笔记：`2026-08-28_16-12-25_3D-nav-dev.md`

### 本次完成
将直接 ROS 2 Launch 的默认 MuJoCo 场景同步为导航脚本使用的特征房间环境，避免两种启动方式加载不同模型。

### 关键改动
- `src/bxi_example_py_elf3/launch/example_demo.launch.py`：默认 `model_file` 改为 `elf3_rooms_nav3d_features.xml`。

### 验证结果
- `python3 -m py_compile src/bxi_example_py_elf3/launch/example_demo.launch.py`：通过。
- `colcon build --merge-install --packages-select bxi_example_py_elf3`：1 个包构建成功。
- `ros2 launch bxi_example_py_elf3 example_demo.launch.py --show-args`：默认模型解析为安装目录下的 `elf3_rooms_nav3d_features.xml`。

---

## 修复 ELF3 一键启动脚本
<!-- obsidian-project-note-source: 2026-08-28_16-54-51_3D-nav-dev.md -->

- 创建时间：`2026-08-28T16:54:37+08:00`
- 原始笔记：`2026-08-28_16-54-51_3D-nav-dev.md`

### 本次完成
修复 `start.sh` 找不到仿真 ROS 接口环境的问题，使一条命令可启动 MuJoCo、ELF3 策略和 RTAB-Map 建图。

### 关键改动
- `start.sh`：统一向策略与建图子进程导出 `/opt/bxi/bxi_ros2_pkg/setup.bash`，同时保留环境变量覆盖能力。

### 验证结果
- `bash -n start.sh ...`：相关 Shell 脚本语法检查通过。
- `./start.sh`：MuJoCo、ELF3 策略与 RTAB-Map 均成功启动；策略进入 `normal`，RGB-D 里程计输出有效。
- 主机进程检查：退出日志跟随后 policy、simulation、mapping 与 RTAB-Map 进程仍在运行。

---

## 为一键启动统一 Fast DDS 大数据配置
<!-- obsidian-project-note-source: 2026-08-28_16-59-35_3D-nav-dev.md -->

- 创建时间：`2026-08-28T16:59:14+08:00`
- 原始笔记：`2026-08-28_16-59-35_3D-nav-dev.md`

### 本次完成
让 `start.sh` 在启动 MuJoCo、策略和建图前统一导出 Fast DDS large-data profile，避免依赖启动终端的当前目录或手工环境配置。

### 关键改动
- `start.sh`：基于项目根目录设置 `FASTDDS_LARGE_DATA_PROFILE`，并同步导出 `FASTRTPS_DEFAULT_PROFILES_FILE` 与 `FASTDDS_DEFAULT_PROFILES_FILE`。

### 验证结果
- `bash -n start.sh` 与 Fast DDS XML 校验：通过。
- 运行进程环境检查：policy 与 mapping 均继承安装目录下的 `fastdds_large_data.xml`。
- `ros2 topic hz`：头部彩色图像约 27.3–27.5 Hz，CameraInfo 约 28.3–28.6 Hz；配置已生效，但满负载下未达到严格 30 Hz。

---

## ELF3 单层室内仿真地图
<!-- obsidian-project-note-source: 2026-08-28_18-06-23_3D-nav-dev.md -->

- 创建时间：`2026-08-28T18:06:03+08:00`
- 原始笔记：`2026-08-28_18-06-23_3D-nav-dev.md`

### 本次完成
新增一张 16×12 米的平面室内 MuJoCo 地图，并将其设为 ELF3 仿真和一键启动流程的默认环境；室内包含分区门洞、家具、障碍物和视觉特征标记，可直接用于 RTAB-Map 建图。

### 关键改动
- `src/bxi_example_py_elf3/data/mujoco_simulation/terrains/indoor_flat.xml`：定义平面地板、外墙、内墙门洞、家具和高对比视觉标记。
- `src/bxi_example_py_elf3/data/mujoco_simulation/elf3_indoor_flat.xml`：生成包含 ELF3 机器人和新室内地形的完整模型。
- `src/bxi_example_py_elf3/launch/example_demo.launch.py`：将直接 ROS2 启动的默认模型切换为新室内地图。
- `src/bxi_example_nav3d/scripts/run_elf3_sim_policy.sh`：将 `./start.sh` 策略启动链路的默认模型切换为新室内地图。

### 验证结果
- `mujoco.MjModel.from_xml_path(...)`：源码及安装目录模型均成功加载，包含 90 个 geom 和 6 个 sensor。
- `colcon build --merge-install --packages-select bxi_example_py_elf3`：构建成功。
- `./start.sh`：MuJoCo、ELF3 策略和 RTAB-Map 均成功启动，运行参数确认加载 `elf3_indoor_flat.xml`。
- `ros2 topic hz /simulation/head_depth_camera/color/image_raw --window 60`：Fast DDS 配置下实测约 29.9 Hz。
- MuJoCo 离屏俯视渲染：墙体、门洞、家具、视觉标记和出生点布局检查通过。

---

## 增强室内仿真场景特征点
<!-- obsidian-project-note-source: 2026-08-28_18-20-46_3D-nav-dev.md -->

- 创建时间：`2026-08-28T18:20:25+08:00`
- 原始笔记：`2026-08-28_18-20-46_3D-nav-dev.md`

### 本次完成
为单层室内 MuJoCo 场景增加墙面纹理和方向可区分的高对比标记，减少大面积光滑纯色区域，提高头部相机用于视觉里程计和 SLAM 的特征密度。

### 关键改动
- `src/bxi_example_py_elf3/data/mujoco_simulation/terrains/indoor_flat.xml`：加入 MuJoCo 内建墙面纹理，以及红、蓝、绿、黄四种不同重复比例的高对比方向标记。
- `src/bxi_example_py_elf3/data/mujoco_simulation/elf3_indoor_flat.xml`：重新生成包含增强地形的完整 ELF3 模型。

### 验证结果
- MuJoCo 离屏渲染与 OpenCV ORB 检测：头部相机初始画面特征点由约 283 个提升到 853 个。
- `colcon build --merge-install --packages-select bxi_example_py_elf3`：构建成功，源码与安装目录模型一致。
- `mujoco.MjModel.from_xml_path(...)`：安装后的模型成功加载，包含 90 个 geom、13 个 material 和 6 个 sensor。

---

## 修复 MuJoCo 室内场景的 RTAB-Map 里程计丢失
<!-- obsidian-project-note-source: 2026-08-28_18-51-42_3D-nav-dev.md -->

- 创建时间：`2026-08-28T18:51:23+08:00`
- 原始笔记：`2026-08-28_18-51-42_3D-nav-dev.md`

### 本次完成
定位并消除了机器人转向时因大面积空白墙和重复纹理导致的视觉匹配盲区，使 RTAB-Map 在正常导航转速下连续经过问题朝向时不再丢失或自动重置。

### 关键改动
- `src/bxi_example_py_elf3/data/mujoco_simulation/terrains/indoor_flat.xml`：使用非周期、不对称的近距隔墙、门洞远端和南墙标记补足有深度的视觉角点。

### 验证结果
- `xmllint --noout .../indoor_flat.xml .../elf3_indoor_flat.xml`：XML 校验通过。
- `colcon build --merge-install --packages-select bxi_example_py_elf3`：构建通过。
- MuJoCo + RTAB-Map 原地右转回归：实际旋转 225.2°，885 个样本最低质量 41，丢失 0 次、自动重置 0 次、数据超时 0 次，最大延迟 49.3 ms。
